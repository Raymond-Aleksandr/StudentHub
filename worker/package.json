{
  "name": "studenthub-syllabus-parser-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --import tsx src/index.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "wrangler": "^4.50.0"
  }
}
